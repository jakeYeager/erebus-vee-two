[project]
name = "erebus-vee-two"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "numpy",
    "pandas",
    "scipy",
    "matplotlib",
    "pyproj",
    "cartopy",
]

[tool.uv]
package = false
